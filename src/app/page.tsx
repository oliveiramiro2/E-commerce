import React from "react";
import { HomeTemplate } from "@/templates";

const Home: React.FC = () => (
    <main className="min-h-screen">
        <HomeTemplate />
    </main>
);

export default Home;
