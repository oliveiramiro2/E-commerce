import React from "react";
import { Header } from "@/components/Header";

export const HomeTemplate: React.FC = () => (
    <div className="bg-white h-screen w-screen flex flex-col items-center">
        <Header />
        <div className="">teste papai2</div>
    </div>
);
