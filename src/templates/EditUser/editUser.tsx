import React from "react";
import { DefaultTemplate } from "../default";

export const EditUser: React.FC = () => {
    return (
        <DefaultTemplate>
            <div>editUser</div>
        </DefaultTemplate>
    );
};
